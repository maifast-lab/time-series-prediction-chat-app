NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Public Google OAuth client ID used by Google Identity Services in the browser.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Admin API base URL. This app stores no business data locally and depends on
# this API for auth, chats, suggestions, sheet rows, and data-source records.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Admin API path that exchanges a Google credential for application tokens.
NEXT_PUBLIC_AUTH_GOOGLE_EXCHANGE_PATH=/api/auth/google

# Python cleaner API used before saving uploaded CSV/XLSX data through Admin.
NEXT_PUBLIC_PYTHON_BACKEND_URL=http://127.0.0.1:8000/api/
